package ex20221121

fun main() {

    // 1) 대입 연산자 ( = )
    // 🍎🍎🍎🍎🍎🍎
    var numOfApple = 6 // 현재 사과의 갯수

    // 2) 복합 대입연산자 ( +=, -=, *=, /=, %= )
    // +🍎🍎
    // numOfApple = numOfApple + 2
    // 회색 밑줄 코드를 줄일 수 있음을 의미함 -> 클린 코드
    numOfApple += 2

    // 값을 누적시킬 때!
    numOfApple -= 2
}